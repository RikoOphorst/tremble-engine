#include "player_ready_packet_handler.h"
#include "networking_demo.h"
#include "../network_object_creator.h"
#include "../game_packet_defs.h"

namespace tremble
{
    PlayerReadyPacketHandler::PlayerReadyPacketHandler()
    {

    }

    PlayerReadyPacketHandler::~PlayerReadyPacketHandler()
    {

    }

    void PlayerReadyPacketHandler::Handle(int packet_id, RakNet::Packet * packet, Peer * sender)
    {

    }
}